import React, { FC } from 'react';
import Button from '../../../packages/components/button';

const Home: FC = () => {
  return (
    <div>
      Hello, Next.js Data <Button />
    </div>
  );
};

export default Home;
